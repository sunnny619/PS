'''
def dfs(현재_상태):
    # 1. [종료 조건 / 정답 확인] 
    # 목표한 상태에 도달했거나 탈락 조건을 만족하면 리턴
    if 종료_조건_만족():
        정답_처리()
        return

    # 2. [다음 상태 탐색]
    # 갈 수 있는 다음 후보들을 하나씩 확인
    for 다음_후보 in 선택지_목록:
        
        # 3. [유효성 검사 및 방문/선택]
        if 이동_가능한가(다음_후보):
            선택_처리(다음_후보)       # 상태 변경 (예: visited 표시, append)
            
            # 4. [재귀 호출]
            dfs(다음_상태)             # 더 깊이 들어감
            
            # 5. [백트래킹 / 원상복구]
            선택_취소(다음_후보)       # 돌아왔을 때 이전 상태로 복원 (예: pop)
'''

def dfs(현재상태):
    종료 조건
    if 종료 조건 만족:
        정답 처리
        return

    for 다음 후보 in 선택지 목목:
        if 이동 가능한가(다음 후보):
            선택 처리(다음 후보)
            dfs(다음 상태)
            선택 취소(다음 후보)
